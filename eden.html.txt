<!DOCTYPE html>
<html lang="mk">
<head>
  <meta charset="UTF-8">
  <title>Factorial</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #1e1e2e;
      color: #cdd6f4;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
    }

    h1 { color: #cba6f7; margin-bottom: 1rem; }

    input {
      padding: 0.5rem 1rem;
      border-radius: 8px;
      border: 1px solid #cba6f7;
      background: #313244;
      color: #cdd6f4;
      font-size: 1rem;
      width: 120px;
      text-align: center;
    }

    button {
      margin-left: 0.5rem;
      padding: 0.5rem 1.2rem;
      border-radius: 8px;
      border: none;
      background: #cba6f7;
      color: #1e1e2e;
      font-weight: bold;
      cursor: pointer;
      font-size: 1rem;
    }

    button:hover { background: #a78bfa; }

    .result {
      margin-top: 1.5rem;
      font-size: 1.4rem;
      color: #a6e3a1;
    }

    .error { color: #f38ba8; }
  </style>
</head>
<body>

  <h1>Факториел</h1>

  <div>
    <input type="number" id="num" placeholder="n" min="0" max="20" />
    <button onclick="calculate()">Пресметај</button>
  </div>

  <div class="result" id="result"></div>

  <script>
    function factorial(n) {
      if (n <= 1) return 1;
      return n * factorial(n - 1);
    }

    function calculate() {
      const n = parseInt(document.getElementById('num').value);
      const res = document.getElementById('result');

      if (isNaN(n) || n < 0) {
        res.className = 'result error';
        res.textContent = 'Внеси позитивен број!';
      } else if (n > 20) {
        res.className = 'result error';
        res.textContent = 'Максимум е 20!';
      } else {
        res.className = 'result';
        res.textContent = n + '! = ' + factorial(n);
      }
    }

    document.getElementById('num').addEventListener('keydown', e => {
      if (e.key === 'Enter') calculate();
    });
  </script>

</body>
</html>